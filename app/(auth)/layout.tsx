/**
 * Auth Layout
 *
 * Layout for authentication-related pages (login, etc.).
 * Provides a simple, clean layout without navigation.
 */

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
